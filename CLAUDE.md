@AGENTS.md

SEGENTIX copy rules:

- Public wording is SEGENTIX / AI governance focused.
- Avoid named competitor callouts in public page copy.
- Use `SEGENTIX` consistently, with `v0.1` as a separate prototype label.
